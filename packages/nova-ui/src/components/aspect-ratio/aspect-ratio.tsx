import { AspectRatio as PrimitiveAspectRatio } from '@novaui/primitives';

import type { AspectRatioProps } from './types';

export default function AspectRatio(props: AspectRatioProps) {
  return <PrimitiveAspectRatio {...props} />;
}
